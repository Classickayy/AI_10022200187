# Web Application Framework
streamlit>=1.28.0,<2.0.0

# Embeddings — fastembed uses ONNX Runtime (no PyTorch needed, ~50 MB vs ~600 MB)
fastembed>=0.2.0           # Drop-in replacement for sentence-transformers embedding
faiss-cpu>=1.7.4
numpy>=1.24.0,<2.0.0

# Cross-encoder reranker (lighter ONNX-based alternative; lazy-loaded only if reranking is on)
# NOTE: if memory is still tight on Render, set use_reranking=False in the sidebar
onnxruntime>=1.16.0        # Runtime used by fastembed — already a transitive dep

# Large Language Model API
groq>=0.4.0                # Official Groq API client
openai>=1.0.0              # Keep for optional OpenAI fallback

# Data Processing & Document Parsing
pandas>=2.0.0
pdfplumber>=0.10.0         # PDF text extraction (budget document)

# Text Processing
nltk>=3.8.0                # Query expansion (WordNet)
regex>=2023.0.0            # Pattern matching in chunking

# Utilities
python-dotenv>=1.0.0       # .env file support
tqdm>=4.65.0               # Progress bars
requests>=2.31.0           # HTTP client

# REMOVED to stay under Render's 512 MB RAM limit:
#   torch, torchvision, sentence-transformers, scikit-learn, tiktoken, python-docx